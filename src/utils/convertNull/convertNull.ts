// answer by src/convert.ts




/**
 * 處理空值轉為null
 * ----
 * 必須完成 Input number, Input string, Input undefined 這三個測試綠燈通過
 * 必須為 emptyToNull 加上型別,
 *     [條件]
 *     傳入參數型別 必須是 number|string|null|undefined
 *     回傳參數型別 必須是 傳入參數
 *
 *
 * @param value 需要被轉換的值
 * @return 判斷若為 ''|0|undefined 則回傳null, 推斷型別也需要正確
 */
export function emptyToNull(value) {
    return value;
}
